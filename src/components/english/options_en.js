import * as React from "react";
import { Button, View, Text } from "react-native";

function OptionsEn({ navigation }) {
    return (
        <View
            style={{ flex: 1, alignItems: "center", justifyContent: "center" }}
        >
            <Text>Options Screen</Text>
        </View>
    );
}

export default OptionsEn;